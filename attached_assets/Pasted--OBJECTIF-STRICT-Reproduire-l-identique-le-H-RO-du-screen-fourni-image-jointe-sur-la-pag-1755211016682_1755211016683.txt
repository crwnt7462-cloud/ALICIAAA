🎯 OBJECTIF (STRICT)
Reproduire **à l’identique** le HÉRO du screen fourni (image jointe) sur la page / (la home `/`).
- NE PAS créer de nouvelle page.
- NE PAS modifier le routing.
- NE PAS altérer les autres sections sous le héros.
- Remplacer uniquement le bloc “Hero” existant par le code ci‑dessous.
- Desktop + mobile fidèles au millimètre (mêmes proportions, interlignes, rayons, ombres).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1) OÙ INTERVENIR
- Ouvre la page **slash** (la home). Selon le projet, c’est souvent l’un des fichiers :
  - `client/src/pages/Slash.tsx` ou `client/src/pages/HomePage.tsx`
  - `client/src/App.tsx` si la home est inline
  - (Si Next/Remix) : `app/routes/_index.tsx` ou `pages/index.tsx`
- Tu DOIS modifier **le bloc "Hero" uniquement**. Si un composant `Hero` existe, remplace son contenu par le code fourni.
- Garde tout le reste (KPI, sections, footer) tel quel.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2) ASSETS & POLICES
- Police : utiliser système iOS‑like (SF / Inter / system-ui). Si Inter dispo : `font-family: Inter, -apple-system, system-ui, ...`
- Aucun import d’images requis : le “téléphone” est stylisé en CSS (verre + ombres) et sert de placeholder. S’il existe un mockup PNG du téléphone, tu peux le placer dans `.heroPhone__screen` en `background-image`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3) CODE À POSER — JSX (remplace le bloc Hero de la page "slash")
Créer/écraser un composant local `HeroSlash` dans la page et appeler `<HeroSlash/>` à la place de l’ancien héros.

///// Début JSX /////
function HeroSlash() {
  return (
    <section className="heroSlash">
      <div className="heroSlash__inner">
        {/* Colonne gauche */}
        <div className="heroSlash__left">
          <div className="heroSlash__badge">
            <span className="dot" />
            <span>Réservation instantanée</span>
          </div>

          <h1 className="heroSlash__title">
            Réservez votre<br/> rendez‑vous <span className="light">beauté</span>
          </h1>

          <p className="heroSlash__subtitle">
            Trouvez et réservez chez les meilleurs professionnels près de chez vous
          </p>

          {/* Barre de recherche + CTA */}
          <div className="heroSlash__search">
            <div className="heroSlash__input">
              <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M10.5 18a7.5 7.5 0 1 1 5.303-12.803A7.5 7.5 0 0 1 10.5 18Zm8-1 4 4" stroke="currentColor" strokeWidth="1.5" fill="none" strokeLinecap="round"/></svg>
              <input placeholder="Rechercher un salon" />
            </div>
            <button className="heroSlash__cta">Rechercher un salon</button>
          </div>

          {/* KPIs alignés (ligne de base du screen) */}
          <ul className="heroSlash__kpis">
            <li><strong>50 000+</strong><span>Rendez‑vous / mois</span></li>
            <li><strong>2 500+</strong><span>Salons partenaires</span></li>
            <li><strong>4,9/5</strong><span>Satisfaction client</span></li>
            <li><strong>24h/24</strong><span>Réservation dispo</span></li>
          </ul>
        </div>

        {/* Colonne droite — téléphone glass */}
        <div className="heroSlash__right">
          <div className="heroPhone">
            <div className="heroPhone__screen">
              {/* Optionnel: si tu as un visuel d’app, mets-le en background-image dans le CSS */}
              <div className="heroPhone__pill" />
              <div className="heroPhone__row" />
              <div className="heroPhone__row" />
              <div className="heroPhone__stat" />
              <div className="heroPhone__cta" />
            </div>
          </div>
          {/* Ombre posée derrière le téléphone */}
          <div className="heroSlash__pad" />
        </div>
      </div>
    </section>
  );
}
///// Fin JSX /////


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4) CSS PRÉCIS À AJOUTER (dans le CSS global déjà importé, ex: `client/src/index.css`)
/* Couleurs & tokens (adapter si variables déjà présentes) */
:root{
  --violet:#7A4DFF;
  --violet-2:#B37DFF;
  --ink:#0E1320;
  --ink-dim:#5B6475;
  --glass: rgba(255,255,255,.62);
  --glass-border: rgba(255,255,255,.38);
  --bg-grad: radial-gradient(1600px 800px at 18% -10%, #F3EFFF 0%, #FFFFFF 58%);
}

/* Section hero: fond, grille et marges identiques au screen */
.heroSlash{
  background: var(--bg-grad);
  padding: 56px 24px 28px;
}
.heroSlash__inner{
  max-width: 1180px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1.05fr 0.95fr;
  align-items: center;
  gap: 36px;
  position: relative;
}

/* BLOC GAUCHE */
.heroSlash__left{ color: var(--ink); }
.heroSlash__badge{
  display:inline-flex; align-items:center; gap:10px;
  padding: 10px 16px; border-radius: 999px;
  background: linear-gradient(180deg, rgba(255,255,255,.78), rgba(255,255,255,.55));
  border: 1px solid var(--glass-border);
  box-shadow: 0 12px 28px rgba(122,77,255,.15);
  font-weight: 600;
}
.heroSlash__badge .dot{
  width:10px; height:10px; border-radius:50%;
  background: radial-gradient(circle at 30% 30%, var(--violet), var(--violet-2));
  box-shadow: 0 0 0 6px rgba(122,77,255,.15);
}

.heroSlash__title{
  margin: 18px 0 8px;
  font-size: clamp(40px, 6.2vw, 72px);
  line-height: 1.02;
  letter-spacing: -0.01em;
  font-weight: 800;
}
.heroSlash__title .light{
  font-weight: 700;
}

.heroSlash__subtitle{
  color: var(--ink-dim);
  font-size: clamp(16px, 1.4vw, 18px);
  max-width: 640px;
}

/* Search + CTA */
.heroSlash__search{
  margin-top: 18px;
  display: grid; gap: 12px;
  grid-template-columns: 1fr auto;
  align-items: center;
}
.heroSlash__input{
  height: 56px; display:flex; align-items:center; gap:12px;
  padding: 0 14px; border-radius: 12px;
  background: var(--glass);
  backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--glass-border);
}
.heroSlash__input svg{ width:20px; height:20px; color: #8A8FA0; flex: 0 0 auto; }
.heroSlash__input input{
  border:none; outline:none; background:transparent; width:100%;
  font-size:16px; color: var(--ink);
}

.heroSlash__cta{
  height:56px; padding: 0 18px; border:none; border-radius: 14px; cursor:pointer;
  font-weight: 800; color:#fff;
  background: linear-gradient(90deg, var(--violet), var(--violet-2));
  box-shadow: 0 10px 26px rgba(122,77,255,.35);
  transition: transform .15s ease, background-position 3s linear;
  background-size: 200% auto;
}
.heroSlash__cta:hover{ background-position: 200% center; }
.heroSlash__cta:active{ transform: scale(.98); }

/* KPIs (ligne sous le bloc, aligné au visuel) */
.heroSlash__kpis{
  margin-top: 22px; padding: 0; list-style:none;
  display:grid; grid-template-columns: repeat(4, minmax(0,1fr)); gap: 12px;
}
.heroSlash__kpis li{ display:grid; gap:2px; }
.heroSlash__kpis strong{ font-size: 22px; font-weight: 800; }
.heroSlash__kpis span{ color: var(--ink-dim); font-size: 14px; }

/* BLOC DROIT — téléphone glass posé sur un pad */
.heroSlash__right{ position: relative; display:flex; justify-content:center; align-items:center; }
.heroSlash__pad{
  position:absolute; inset:auto 0 -8px 0; height: 180px; filter: blur(24px);
  background: radial-gradient(50% 70% at 50% 40%, rgba(122,77,255,.18), rgba(179,125,255,.08) 40%, transparent 70%);
  z-index: 0;
}
.heroPhone{
  position: relative; z-index: 1;
  width: 360px; height: 720px; border-radius: 38px;
  background: linear-gradient(180deg, rgba(255,255,255,.8), rgba(255,255,255,.62));
  border:1px solid var(--glass-border);
  backdrop-filter: blur(18px); -webkit-backdrop-filter: blur(18px);
  box-shadow: 0 22px 50px rgba(20,0,60,.22);
  transform: rotate(-8deg);
}
.heroPhone__screen{
  position:absolute; inset: 14px; border-radius: 30px; overflow:hidden;
  background: linear-gradient(180deg, rgba(245,245,255,.9), rgba(240,240,255,.65));
  border: 1px solid rgba(255,255,255,.6);
}
/* mini éléments pour rappeler le screen */
.heroPhone__pill{
  width: 62%; height: 34px; margin: 18px auto 10px; border-radius: 14px;
  background: linear-gradient(180deg, rgba(255,255,255,.85), rgba(255,255,255,.6));
  border:1px solid rgba(255,255,255,.7);
  box-shadow: 0 8px 20px rgba(0,0,0,.06);
}
.heroPhone__row{
  width: 80%; height: 38px; margin: 8px auto; border-radius: 12px;
  background: linear-gradient(180deg, rgba(255,255,255,.8), rgba(255,255,255,.55));
  border:1px solid rgba(255,255,255,.7);
}
.heroPhone__stat{
  width: 64%; height: 54px; margin: 14px auto; border-radius: 14px;
  background: linear-gradient(180deg, rgba(255,255,255,.85), rgba(255,255,255,.58));
  border:1px solid rgba(255,255,255,.7);
}
.heroPhone__cta{
  width: 72%; height: 44px; margin: 18px auto; border-radius: 14px;
  background: linear-gradient(90deg, var(--violet), var(--violet-2));
  box-shadow: 0 10px 26px rgba(122,77,255,.35);
}

/* RESPONSIVE: reproduire la version mobile du screen (stack) */
@media (max-width: 980px){
  .heroSlash__inner{
    grid-template-columns: 1fr;
    gap: 28px;
  }
  .heroSlash__right{
    order: -1; /* téléphone au-dessus sur mobile si nécessaire */
    transform: translateY(6px);
  }
  .heroPhone{ transform: rotate(0deg); width: 320px; height: 640px; margin: 0 auto; }
  .heroSlash__kpis{ grid-template-columns: repeat(2,1fr); }
}
@media (max-width: 560px){
  .heroSlash{ padding: 40px 16px 22px; }
  .heroSlash__title{ font-size: clamp(34px, 8vw, 44px); }
  .heroSlash__search{ grid-template-columns: 1fr; }
}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5) INTÉGRATION
- Dans la page "slash", **importer** le CSS global si ce n’est pas déjà fait (ex: `import '../index.css'`).
- Rendre `<HeroSlash/>` à l’emplacement EXACT où se trouve l’ancien héros.
- Laisser intacts les composants/sections sous le héros.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6) CONTRÔLES QUALITÉ (visuels)
- Comparer cote‑à‑cote avec le screen :
  - Alignement du badge, interlignes du H1 (deux lignes), poids des mots, sous‑titre max‑width ≈ 640px.
  - Hauteur des inputs/CTA = 56px, rayons 12–14px, ombres douces violettes.
  - Téléphone : bords 38px, légère rotation (~‑8° desktop), blur et halo violet sous l’appareil.
  - KPIs en 4 colonnes desktop, 2 colonnes mobile, typographies correspondantes.
- Tester widths: 1440px, 1280px, 1024px, 390px. Les proportions doivent rester “identiques à l’œil”.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7) COMMIT
- Commit unique :
  git add -A
  git commit -m "feat(ui): replicate hero of slash page pixel-perfect (desktop+mobile)"