import SalonPage from '@/pages/SalonPage';

export default function SalonDynamicPage() {
  // RÈGLE ABSOLUE: Utilisation du template officiel /salon pour TOUS les salons
  // Aucune personnalisation autorisée - Template intouchable
  return <SalonPage />;
}