import SalonPageTemplate from '@/components/SalonPageTemplate';

/**
 * PAGE SALON AVYENTO - TEMPLATE UNIQUE
 * 
 * Cette page utilise maintenant le SalonPageTemplate unifié
 * basé sur le design parfait enregistré le 21 Août 2025
 * 
 * Toutes les pages /salon/ utilisent désormais ce template unique
 */
export default function SalonPage() {
  // Utilise le template Avyento par défaut
  return <SalonPageTemplate />;
}